import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.TBColorBlindness',
  appName: 'Best Color Blindness Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
